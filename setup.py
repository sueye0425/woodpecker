from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This repo predicts if a device reading is a failure or not, in order to decide the need for maintenance',
    author='Sue Ye',
    license='MIT',
)
